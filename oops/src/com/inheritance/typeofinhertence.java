package com.inheritance;


public class typeofinhertence {
    //1: one class extends another class : single inheritance
    //1.2 one class can inherit from derived class  and then that class becomes parent class from another class
    // 2 one class extending more than one classes
    // multiple inheritance is not supported in java

    // one class is inherited by many classes : hierarchial
    // combination of single and multiple inheritance is hybrid inheritance





}
